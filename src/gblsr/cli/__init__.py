"""Command-line entry points for the GB-LSR package.

Each submodule here exposes a ``main(argv=None)`` function suitable
both for direct invocation (``python -m gblsr.cli.<name>``) and as a
console-script entry registered in ``pyproject.toml``
(``[project.scripts]`` -> ``gblsr-<name>``).
"""
